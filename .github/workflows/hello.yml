name: My First Workflow

on: [push]

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello, world!"

  build:
      runs-on: ubuntu-latest
      needs: my-job
      steps:
        - run: echo "Building..."

  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Testing..."